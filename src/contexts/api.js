/**
 * The character context controls persistant API-related data.
 * @module [{contexts}api]
 * @default {}
 * @example import { APIContext } from "context/api";
 */
import React from "react";

/**
 * `APIContext` is the context API for users.
 */
export const APIContext = React.createContext({});